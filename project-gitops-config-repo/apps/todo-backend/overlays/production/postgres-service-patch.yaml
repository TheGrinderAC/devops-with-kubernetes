apiVersion: apps/v1
kind: Deployment
metadata:
  name: todo-backend-dep
spec:
  template:
    spec:
      initContainers:
        - name: check-db
          env:
            - name: POSTGRES_SERVICE_NAME
              value: "prod-postgres-service"
            - name: POSTGRES_HOST
              value: "prod-postgres-service"
