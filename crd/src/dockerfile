FROM node:18-alpine

WORKDIR /app

# Copy package files
COPY . .

# Install dependencies
RUN npm ci --only=production

# Create non-root user
RUN addgroup -g 1001 -S nodejs && \
    adduser -S controller -u 1001

USER controller

EXPOSE 8080

CMD ["npm", "start"]